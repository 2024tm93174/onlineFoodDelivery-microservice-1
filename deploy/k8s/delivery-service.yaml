apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: foodgo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: delivery-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
        - name: DATABASE_URL
          value: "postgresql://delivery:deliverypw@delivery-db.foodgo.svc.cluster.local:5432/deliverydb"
          readinessProbe:
            httpGet: { path: /health, port: 80 }
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 80 }
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }

---

apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: foodgo
spec:
  type: ClusterIP
  selector:
    app: delivery-service
  ports:
    - port: 80
      targetPort: 80
